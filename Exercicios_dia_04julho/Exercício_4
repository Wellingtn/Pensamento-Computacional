def calcula_h(n):
    h = 0
    for c in range(1, n+1):
        if c % 2 == 1:
            h += 1/c
        elif c % 2 == 0:
            h -= 1/c
    return h



def main():
    n = int(input('Digite um valor para ser o limite do intervalo: '))
    h = calcula_h(n)
    print(f'{h:.5f}')
main()
